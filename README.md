# Emoji Throughout Time
Draft. Done parts are `discord-export` which was written a while ago and `discord-convert.py`.
To try out discord-export run `> python discord-export/cli.py`, requires logzero and discord.py.

Stay put.
